import 'package:flex_color_scheme/flex_color_scheme.dart';

void main() {
  print(FlexScheme.values.indexOf(FlexScheme.shark));
  print(FlexScheme.values.indexOf(FlexScheme.greyLaw));
  print(FlexScheme.values.indexOf(FlexScheme.sanJuanBlue));
}
